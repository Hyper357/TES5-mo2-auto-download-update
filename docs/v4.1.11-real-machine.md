# v4.1.11 real-machine acceptance

Correctness first: stale local Nexus file identity must not force a re-download when local installed-version evidence already matches the exact-chain target. Such disagreements are held for local identity review.

Performance second: direct-download reports per-stage timings so real-machine bottlenecks are measured before concurrency is increased. The next acceptance run uses a small clean batch, not the full library.
