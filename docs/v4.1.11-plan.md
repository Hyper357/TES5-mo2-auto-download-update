# v4.1.11 plan

1. Tighten Update Eligibility so a stale Nexus fileId / inactive-file signal cannot force an update when local installed-version evidence already matches the target/current branch.
2. Treat exact-chain vs local-current disagreement as HOLD_LOCAL_IDENTITY_CONFLICT instead of UPDATE_CONFIRMED.
3. Speed up direct download by reusing browser/session work where possible and removing unnecessary per-item waits; keep exact fileId, size, archive integrity and VERIFIED gates.
4. Real-machine acceptance uses a small clean batch before full library.
